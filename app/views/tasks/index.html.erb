<h1>TODO</h1>
  <% @tasks.each do |task| %>
    <h2><%= link_to task.name, task_path(task) %></h2>  <!-- was task.id before -->
    <ul>
      <li><%=link_to "edit", edit_task_path(task)%></li>
      <li><%= link_to "Remove task", task_path(task), method: :delete %></li>
    </ul>
  <% end %>
  <p><%= link_to "Create", new_task_path %></p>
<!-- /Users/mac3/code/colbol/rails-task-manager/app/models/task.rb -->
